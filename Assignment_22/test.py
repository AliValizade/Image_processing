import cv2 as cv
import numpy as np

# img = np.full((400, 400), 255, dtype=np.uint8)
# cv.line(img, (-30, 100), (100, -30), 0, 30)
# cv.rectangle(img, (50, 50), (150, 150), 40, 10)
# cv.circle(img, (200, 200), 50, 80, -1)
# points = np.array([[5, 5], [15, 300], [300, 300], [280, 90]], np.int32 )
# cv.polylines(img, [points], True, 60, 5)
# font = cv.FONT_HERSHEY_COMPLEX
# cv.putText(img, 'Hello World!', (50, 350), font, 1, 55, 2, cv.LINE_AA)

# cv.imwrite('img/line.jpg', img)
# cv.imshow('output image', img)
# cv.waitKey()

